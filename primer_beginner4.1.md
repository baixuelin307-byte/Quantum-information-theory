# 4: Introduction to state distinguishing problems 量子态区分问题介绍

声明：当我们物理 bit 转化成为量子 bit 后，量子 bit 的状态就是量子态。但是量子态也会细分很多种状态。

## 1: EX

$|+\rangle = \frac{1}{\sqrt{2}}|0\rangle + \frac{1}{\sqrt{2}}|1\rangle$

$|-\rangle = \frac{1}{\sqrt{2}}|0\rangle - \frac{1}{\sqrt{2}}|1\rangle$

这个例子中只有 plus 和 minus state，这两个都是属于正交态，因为：

$\langle + | - \rangle = 0$

<img width="218" height="205" alt="image" src="https://github.com/user-attachments/assets/ec1b9ad3-c5fd-4dde-8204-226d64211570" />

在二维的横截面上面，不同的符号代表的方向：

- $|1\rangle$ 向上
- $|0\rangle$ 向右
- $|+\rangle$ 右上
- $|-\rangle$ 右下


# 额外解释：如何从量子态公式直接判断方向

对于单量子比特状态：

|ψ⟩ = α|0⟩ + β|1⟩

在当前二维示意图中，可以直接理解成二维坐标：

|ψ⟩ ↔ (α, β)

其中：

- |0⟩：横轴方向
- |1⟩：纵轴方向
- α：横向分量
- β：纵向分量

核心记忆：

**系数的正负决定方向，系数的大小决定偏向。**

---

## 1. 根据正负判断方向

| α | β | 方向 |
|---|---|---|
| 正 | 正 | 右上 ↗ |
| 正 | 负 | 右下 ↘ |
| 负 | 正 | 左上 ↖ |
| 负 | 负 | 左下 ↙ |

---

## 2. 例子：|+⟩

|+⟩ = (1/√2)|0⟩ + (1/√2)|1⟩

直接看两个系数：

α = 1/√2 > 0

β = 1/√2 > 0

所以：

(α, β) = (+, +)

因此方向：

**右上 ↗**

写成列向量：

|+⟩ = (1/√2) [1, 1]ᵀ

也就是：

横向分量 = 1/√2  
纵向分量 = 1/√2

两个方向都是正的，因此指向右上。

---

## 3. 例子：|-⟩

|-⟩ = (1/√2)|0⟩ - (1/√2)|1⟩

也可以写成：

|-⟩ = (1/√2)|0⟩ + (-1/√2)|1⟩

直接看两个系数：

α = 1/√2 > 0

β = -1/√2 < 0

所以：

(α, β) = (+, -)

因此方向：

**右下 ↘**

写成列向量：

|-⟩ = (1/√2) [1, -1]ᵀ

也就是：

横向分量 = 1/√2  
纵向分量 = -1/√2

横向为正，纵向为负，因此指向右下。

---

## 4. 系数大小决定更靠近哪个轴

例如：

|ψ⟩ = (√3/2)|0⟩ + (1/2)|1⟩

两个系数都是正数，因此一定在右上方向。

但是：

√3/2 > 1/2

所以 |0⟩ 的分量更大。

因此这个状态：

- 仍然指向右上；
- 但更靠近 |0⟩ 的方向；
- 也就是更靠近横轴。

反过来：

|ψ⟩ = (1/2)|0⟩ + (√3/2)|1⟩

仍然指向右上，但是 |1⟩ 的分量更大，因此更靠近纵轴。

---

## 5. 看到公式时怎么快速判断

看到：

|ψ⟩ = α|0⟩ + β|1⟩

直接把它看成：

(α, β)

然后：

1. α > 0 → 向右
2. α < 0 → 向左
3. β > 0 → 向上
4. β < 0 → 向下
5. |α| 和 |β| 的大小决定更偏向哪个轴

所以最简单的记忆方式是：

**|ψ⟩ = α|0⟩ + β|1⟩  →  (α, β)**

**正负决定方向，大小决定偏向。**

---

## 6. 不要只看状态名字里的 + 和 -

例如：

|-⟩ = (1/√2)|0⟩ - (1/√2)|1⟩

这里的“-”不是说整个量子态是“负方向”。

真正的意思是：

|1⟩ 前面的系数是负数。

所以：

- |0⟩ 分量为正 → 向右
- |1⟩ 分量为负 → 向下

最终就是：

**右下 ↘**

---

> 注意：
> 这种“上下左右”的判断方法适用于目前讨论的实数系数情况。
> 如果以后系数出现 i、e^(iθ) 等复数，就需要进一步考虑相位，不能只用二维平面的上下左右判断。
